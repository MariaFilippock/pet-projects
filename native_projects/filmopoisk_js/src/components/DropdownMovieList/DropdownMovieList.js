import{Store}from"../../../store.js";import{MAX_VISIBLE_DROPDOWN_FILMS}from"../../../const.js";import{getHTMLElements}from"../../../controller.js";export function renderDropdownMovieList(){const o=getHTMLElements().dropdownContainer;o&&(o.innerHTML=Store.state.isLoadedListVisible?`<div class="dropdown-list-movies" id="dropdown-list-movies">\n                                 ${createDropdownMovieList()}\n                                 </div>`:"")}function createDropdownMovieList(){return Store.state.loadedList.map(o=>`<div class="dropdown-movie-item" id = '${o.id}'>${o.name} \n                    <span class="dropdown-movie-rating">${Number(o.rating.kp)>0?o.rating.kp.toFixed(1):""}</span>\n                </div>`).slice(0,MAX_VISIBLE_DROPDOWN_FILMS).join(" ")}